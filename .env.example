# PostgreSQL Neon - Connection Strings
# ⚠️ REEMPLAZA CON TUS VALORES REALES EN .env
# Solicita las credenciales al equipo por un canal seguro

# Desarrollo (Neon DB)
ConnectionStrings__DefaultConnection=Host=your-dev-host.aws.neon.tech;Database=neondb;Username=neondb_owner;Password=your-password;SSL Mode=Require

# JWT Secret (mínimo 32 caracteres)
Jwt__SecretKey=your-super-secret-jwt-key-min-32-characters-change-in-prod

# Environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development